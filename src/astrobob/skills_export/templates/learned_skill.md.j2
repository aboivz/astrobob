---
name: {{ slug }}
description: {{ description }}
---

{{ content }}

---

## Metadata

**Auto-generated from procedural memory**

- **Memory ID**: `{{ memory_id }}`
- **Created**: {{ created_at.strftime('%Y-%m-%d %H:%M:%S UTC') }}
- **Importance**: {{ '⭐' * importance }}
- **Tags**: {{ tags | join(', ') if tags else 'none' }}
- **Project**: `{{ project }}`
- **Scope**: `{{ scope }}`
- **Confidence**: {{ (confidence * 100) | round(1) }}%
- **Source**: `{{ source }}`
- **Success Count**: {{ success_count }}
- **Access Count**: {{ access_count }}
- **Last Accessed**: {{ last_accessed_at.strftime('%Y-%m-%d %H:%M:%S UTC') if last_accessed_at else 'Never' }}
{% if supersedes %}
- **Supersedes**: `{{ supersedes }}`
{% endif %}

{% if provenance.derived_from %}
### Provenance

**Derived from episodes:**
{% for episode_id in provenance.derived_from %}
- `{{ episode_id }}`
{% endfor %}

{% if provenance.session_id %}
**Session**: `{{ provenance.session_id }}`
{% endif %}

{% if provenance.bob_skill_used %}
**Created using skill**: `{{ provenance.bob_skill_used }}`
{% endif %}
{% endif %}

---

*This skill was automatically generated by AstroBob. To update it, modify the source procedural memory and run `astrobob skills sync` again.*